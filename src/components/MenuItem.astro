---
interface Props {
  item: {
    name?: string;
    price?: string;
    description?: string;
    note?: string;
  };
}

const { item } = Astro.props;
---

<li class="dish">
  <div class="dish-top">
    <span class="dish-name">{item.name}</span>
    <span class="dish-price">{item.price}</span>
  </div>
  {item.description && <p class="dish-desc">{item.description}</p>}
  {item.note && <p class="menu-fine-print dish-note">{item.note}</p>}
</li>

<style>
  .dish {
    padding: 1.25rem 0;
    border-bottom: 1px solid var(--border);
  }

  .dish-top {
    display: flex;
    justify-content: space-between;
    align-items: baseline;
    gap: 1.5rem;
    margin-bottom: 0.4rem;
  }

  .dish-price {
    font-family: var(--font-header-action), sans-serif;
    font-size: var(--text-base);
    font-weight: 300;
    color: var(--ink-mid);
    white-space: nowrap;
    flex-shrink: 0;
  }

  .dish-note {
    margin-top: 0.5rem;
  }
</style>
