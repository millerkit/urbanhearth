---
import { Image } from "astro:assets";
import { getSiteSettings } from "../lib/site-settings";

import DiningRoomHeroPhoto from "../assets/photos/UrbanHearth-52.jpg";
import DiningRoomTeaser from "../assets/photos/DiningRoomTeaser.jpeg";
import ChefsCounterTeaser from "../assets/photos/ChefsCounterTeaser.jpeg";
import BarRendering from "../assets/photos/SalonPhoto.jpeg";
import michelinSeal from "../assets/ui/michelin-selected.png";
import jamesBeardSeal from "../assets/ui/james-beard-seal.png";
import splashLogo from "../assets/brand/UrbanHearth_Logo.png";
import IntroStatement from "../components/IntroStatement.astro";
import Hero from "../components/Hero.astro";
import Base from "../layouts/Base.astro";
import ReservationButton from "../components/ReservationButton.astro";

const restaurant = await getSiteSettings();
const restaurantJsonLd = {
  "@context": "https://schema.org",
  "@type": "Restaurant",
  name: restaurant.name,
  url: restaurant.url,
  telephone: restaurant.phone,
  address: {
    "@type": "PostalAddress",
    streetAddress: "1281 Cambridge St",
    addressLocality: "Cambridge",
    addressRegion: "MA",
    postalCode: "02139",
    addressCountry: "US",
  },
  geo: {
    "@type": "GeoCoordinates",
    latitude: 42.3732,
    longitude: -71.0989,
  },
  openingHoursSpecification: [
    {
      "@type": "OpeningHoursSpecification",
      dayOfWeek: [
        "Tuesday",
        "Wednesday",
        "Thursday",
        "Friday",
        "Saturday",
        "Sunday",
      ],
      opens: "17:00",
      closes: "22:00",
    },
  ],
  servesCuisine: "New American",
  priceRange: "$$$",
  description: restaurant.tagline,
  hasMap: "https://maps.google.com/?q=1281+Cambridge+St+Cambridge+MA",
  sameAs: [
    restaurant.url,
    restaurant.social.facebook,
    restaurant.social.instagram,
  ],
};
---

<Base title="Urban Hearth — Cambridge, MA">
  <Fragment slot="head">
    <script
      type="application/ld+json"
      set:html={JSON.stringify(restaurantJsonLd)}
    />
    <script is:inline>
      (function () {
        const KEY = "uh_splash_ts";
        const SEVEN_DAYS = 7 * 24 * 60 * 60 * 1000;
        try {
          const last = localStorage.getItem(KEY);
          if (last && Date.now() - Number(last) < SEVEN_DAYS) {
            const s = document.createElement("style");
            s.textContent = "#splash{display:none!important}";
            document.head.appendChild(s);
          } else {
            localStorage.setItem(KEY, String(Date.now()));
          }
        } catch (_) {}
      })();
    </script>
  </Fragment>
  <div id="splash" aria-hidden="true">
    <img src={splashLogo.src} alt="Urban Hearth" class="splash-logo" />
    <p class="splash-location">
      <span>Inman Square&nbsp;&nbsp;|&nbsp;&nbsp;Cambridge</span>
    </p>
  </div>

  <Hero src={DiningRoomHeroPhoto} />

  <IntroStatement />

  <section class="dining-teasers">
    <div class="teasers-grid">
      <a href="/dining-areas#dining-room" class="teaser">
        <div class="teaser-photo-wrap">
          <Image
            src={DiningRoomTeaser}
            alt="Dining Room at Urban Hearth"
            class="teaser-photo"
            widths={[400, 800]}
            sizes="(max-width: 780px) 100vw, 33vw"
            loading="lazy"
          />
        </div>
        <div class="teaser-body">
          <h2 class="teaser-title">Dining Room</h2>
        </div>
      </a>

      <div class="teaser-mid-col">
        <a href="/dining-areas#chefs-counter" class="teaser">
          <div class="teaser-photo-wrap">
            <Image
              src={ChefsCounterTeaser}
              alt="Chef's Counter at Urban Hearth"
              class="teaser-photo"
              widths={[400, 800]}
              sizes="(max-width: 780px) 100vw, 33vw"
              loading="lazy"
            />
          </div>
          <div class="teaser-body">
            <h2 class="teaser-title">Chef's Counter</h2>
          </div>
        </a>
      </div>

      <a href="/dining-areas#salon" class="teaser">
        <div class="teaser-photo-wrap">
          <img
            src={BarRendering.src}
            alt="Salon and Bar at Urban Hearth"
            class="teaser-photo"
            loading="lazy"
          />
        </div>
        <div class="teaser-body">
          <h2 class="teaser-title">Salon & Bar</h2>
        </div>
      </a>
    </div>
    <div class="teasers-booking-row">
      <div class="teasers-booking-spacer"></div>
      <div class="teasers-booking-cell">
        <div class="booking-inner">
          <p class="booking-eyebrow section-eyebrow">
            Choose your dining experience
          </p>
          <ReservationButton label="Reserve" />
        </div>
      </div>
      <div class="teasers-booking-spacer"></div>
    </div>
  </section>

  <section class="awards">
    <div class="awards-inner">
      <img src={michelinSeal.src} alt="Michelin Selected" class="award-img" />
      <img src={jamesBeardSeal.src} alt="James Beard Award" class="award-img" />
    </div>
  </section>
</Base>

<style>
  /* ── Dining teasers ── */
  .dining-teasers {
    padding: 2.5rem 5rem;
  }

  .teasers-grid {
    display: flex;
    gap: 1.5rem;
    background: var(--warm-white);
    align-items: center;
  }

  .teaser {
    background: var(--midnight);
    flex: 1;
    min-width: 0;
    display: flex;
    flex-direction: column;
    text-decoration: none;
    overflow: hidden;
  }

  .teaser-mid-col {
    flex: 1;
    min-width: 0;
  }

  .teaser-mid-col .teaser-photo-wrap {
    aspect-ratio: 3 / 4;
  }

  .teasers-booking-row {
    display: flex;
    gap: 1.5rem;
    padding-top: 2rem;
  }

  .teasers-booking-spacer {
    flex: 1;
  }

  .teasers-booking-cell {
    flex: 1;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.75rem;
  }

  .booking-inner {
    display: inline-flex;
    flex-direction: column;
    gap: 0.75rem;
    align-items: stretch;
  }

  .booking-eyebrow {
    color: var(--ink-mid);
    text-align: center;
    margin-top: 1.5rem;
  }

  .teasers-booking-cell :global(.reservation-btn) {
    background: var(--midnight);
    color: var(--parchment);
    font-size: 16px;
  }

  .teaser-photo-wrap {
    position: relative;
    width: 100%;
    aspect-ratio: 1 / 1;
    overflow: hidden;
    background: var(--midnight);
    flex-shrink: 0;
  }

  .teaser-photo {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center;
    display: block;
    transition: transform 0.5s ease;
  }

  .teaser:hover .teaser-photo {
    transform: scale(1.03);
  }

  .teaser-body {
    padding: 1.75rem 1.75rem 2.25rem;
    text-align: center;
  }

  .teaser-title {
    font-family: var(--font-body) serif;
    font-size: clamp(1.4rem, 2vw, 1.9rem);
    font-weight: 300;
    letter-spacing: 0.06em;
    color: var(--parchment);
    line-height: 1.1;
    margin-bottom: 0.9rem;
    transition: opacity 0.2s;
  }

  .teaser:hover .teaser-title {
    opacity: 0.7;
  }

  /* ── Awards ── */
  .awards {
    background: var(--warm-white);
    padding: 2rem 2rem 2.5rem;
    border-bottom: 1px solid var(--border);
  }

  .awards-inner {
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 2.5rem;
    max-width: 500px;
    margin: 0 auto;
  }

  .award-img {
    width: auto;
    object-fit: contain;
    display: block;
    opacity: 0.8;
  }

  .award-img:first-child {
    height: 96px;
  }

  .award-img:last-child {
    height: 120px;
  }

  /* ── Responsive ── */
  @media (max-width: 780px) {
    .teasers-grid {
      flex-direction: column;
    }
  }

  @media (max-width: 500px) {
    .teaser-body {
      padding: 1.25rem 1.25rem 1.75rem;
    }

    .awards-inner {
      gap: 2.5rem;
    }

    .award-img {
      height: 72px;
    }
  }
</style>

<style is:global>
  /* ── Splash overlay ── */
  #splash {
    position: fixed;
    inset: 0;
    z-index: 9999;
    background: var(--parchment);
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    animation: splash-out 0.6s ease 1.6s forwards;
  }

  #splash .splash-logo {
    width: min(320px, 58vw);
    height: auto;
    mix-blend-mode: multiply;
    filter: contrast(1.4);
    animation: splash-logo-in 0.7s ease forwards;
  }

  #splash .splash-location {
    display: flex;
    align-items: center;
    margin-top: -6rem;
    animation: splash-logo-in 0.7s ease 0.3s both;
  }

  #splash .splash-location span {
    font-family: var(--font-header-action), sans-serif;
    font-size: 11px;
    font-weight: 400;
    letter-spacing: 0.25em;
    text-transform: uppercase;
    color: var(--charcoal);
  }

  @keyframes splash-logo-in {
    from {
      opacity: 0;
      transform: translateY(6px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes splash-out {
    from {
      opacity: 1;
    }
    to {
      opacity: 0;
    }
  }
</style>

<script>
  const splash = document.getElementById("splash");
  if (splash) {
    splash.addEventListener("animationend", (e) => {
      if (e.animationName === "splash-out") {
        splash.remove();
      }
    });
  }
</script>
